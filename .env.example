# Database Configuration
# Set DB_PROVIDER to 'supabase' or 'neon'
DB_PROVIDER=supabase

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# PostgreSQL Neon Serverless Configuration (for future use)
# To use Neon instead of Supabase:
# 1. Set DB_PROVIDER=neon
# 2. Install pg package: npm install pg
# 3. Uncomment and fill in DATABASE_URL below
# DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require
